;;; init-lua.el --- Lua settings -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:


(use-package lua-mode
  :straight t
  ;; `auto-mode-alist' entry for `lua-mode' is autoloaded
  :defer t)


(provide 'init-lua)
;;; init-lua.el ends here
