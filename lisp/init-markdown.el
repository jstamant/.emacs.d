;;; init-markdown.el --- Markdown settings -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:


(use-package markdown-mode
  :ensure t
  ;; `auto-mode-alist' entries for `markdown-mode' are autoloaded
  :defer t)


(provide 'init-markdown)
;;; init-markdown.el ends here
