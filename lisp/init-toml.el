;;; init-toml.el --- TOML settings -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:


(use-package toml-mode
  :straight t
  ;; `auto-mode-alist' entry for `toml-mode' is autoloaded
  :defer t)


(provide 'init-toml)
;;; init-toml.el ends here
